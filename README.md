# electron-website-dashboard
Display multiple websites in an application.

# Installation

1. `npm install`
2. `npm run build`
3. `npm run up` for test
4. Copy `./docker-compose-scripts/docker-compose-up.sh` to another folder and customize it.
